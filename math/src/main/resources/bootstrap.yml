spring:
  application.name: math
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5671/eureka
